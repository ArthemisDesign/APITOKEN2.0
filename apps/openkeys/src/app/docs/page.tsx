import { DocsPortal } from "./docs-portal";

export const metadata = { title: "Документация — OpenKeys" };

/** Тот же портал документации, что и в дашборде: один источник правды по подключению. */
export default function DocsPage() {
  return <DocsPortal />;
}
