# macOS metadata
.DS_Store

# Rust
/target/

# TypeScript commercial platform
node_modules/
dist/
.turbo/
.pnpm-store/
coverage/
*.tsbuildinfo
.next/
.artifacts/

# СЕКРЕТЫ — никогда в репозиторий
*.env
!*.env.example
*.gpg
# Google service-account ключи (GSC/sitemap auto-submit) — только на сервере, не в git
apitoken-*.json
*service-account*.json
*-service-account-*.json
subscriptions.json
subscriptions.db
subscriptions.db-*
active_profile
active_proxy
.credentials.json
*.token
*.fifo
.claude-*/
.claude/worktrees/
secrets*

# Local database volumes
.data/

# Локальное состояние AI-агентов (OpenCode: goals, plugins) — не коммитить
.opencode/
