# Dormant operator utility timer. Production does not enable it until capture credentials and
# blue-green activation have their own reviewed safe path (docs/ops/INFRASTRUCTURE.md).
[Unit]
Description=Daily refresh of claude-api fingerprint values

[Timer]
OnCalendar=daily
Persistent=true
RandomizedDelaySec=1h

[Install]
WantedBy=timers.target
